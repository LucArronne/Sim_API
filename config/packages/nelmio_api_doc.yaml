nelmio_api_doc:
    documentation:
        servers:
            - url: https://localhost:8000
              description: API SimRacing
        info:
            title: SimRacing API
            description: API Documentation pour SimRacing
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        default:
            path_patterns:
                - ^/api(?!/doc$|/doc.json$)
